# Changesets

This folder is where we keep changesets for `hsc-fetch`.

- Run `pnpm changeset` after meaningful changes to record the version bump (major/minor/patch) and summary.
- The generated markdown files live here until the release workflow consumes them.
- Do not edit `config.json` manually unless you know what you are doing.
